---
name: "DangerJS Github Action Harness"
author: "J. Patrick Fulton (jpfulton)"
description: "Runs DangerJS within a Docker container in a Github Action"
branding:
  icon: "check-square"
  color: "green"
inputs:
  dangerfile:
    description: "The path of the Dangerfile to run"
    required: true
  debug_mode:
    description: "Whether to run action in debug mode"
    required: false
    default: "false"
  github_token:
    description: "The Github token to use for DangerJS"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.dangerfile }}
    - ${{ inputs.debug_mode }}
    - ${{ inputs.github_token }}
